(
 (nil . ((indent-tabs-mode . t)
				 (tab-width . 2) ))
 (c++-mode . (
							(compile-command . "(cd ../../build && make && make install && ./bin/jUI_SDL )")))
 ((c-mode . (
						 (compile-command . "(cd ../../build && make && make install && ./bin/jUI_SDL )")))))
